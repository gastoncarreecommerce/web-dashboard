{
  "_origen": [
    "ENDPOINT CONFIRMADO por la API: /v2/serve/user/search devuelve 422 (existe y",
    "valida), mientras /v2/serve/user/semantic_search y /v2/search/user/search dan",
    "404. El `choose` estandar tambien responde, pero no es el de busqueda.",
    "",
    "LA DOC DE DY SE CONTRADICE, y la API decidio:",
    "",
    "  · El ejemplo de 'Experience Search' pone selector, user, session, context y",
    "    options TODOS anidados adentro de `query`.",
    "  · El ejemplo de 'Autosuggest' —misma API, segun la doc 'maintains the same",
    "    structure as the Search API'— los pone en el NIVEL DE ARRIBA, y deja",
    "    adentro de `query` solo text/suggestions.",
    "",
    "Con todo anidado adentro de `query`, la API responde:",
    "    HTTP 422 { error: { message: 'request must contain context', type:",
    "    'badRequest' } }",
    "",
    "O sea que `context` va arriba. Se sigue la forma del ejemplo de Autosuggest:",
    "`query` lleva SOLO lo propio de la busqueda (text, pagination, y si algun dia",
    "hicieran falta, filters/sortBy/facets) y user/context/selector/options van en",
    "el nivel de arriba.",
    "",
    "`session` se omite: el error no la pide y DY la crea sola (devuelve la cookie",
    "_dyjsession). Clavar una en el archivo tampoco serviria: vence a los 30 min.",
    "",
    "La API key va en la env var DY_API_KEY.",
    "",
    "`context.page.data` es OBLIGATORIO aunque vaya vacio. Sin el:",
    "    HTTP 422 { error: { message: 'request must contain context page data' } }",
    "La plantilla que da el panel lo traia como [] y se perdio al reescribir el body",
    "siguiendo la doc, que no lo menciona. En una pagina de producto llevaria el SKU",
    "del producto mirado; para una consulta de diagnostico va vacio."
  ],
  "endpoint": "https://dy-api.com/v2/serve/user/search",
  "selector": "Semantic Search",
  "body": {
    "user": {
      "active_consent_accepted": true,
      "dyid": "7530018819271298053"
    },
    "query": {
      "text": "{{query}}",
      "pagination": {
        "numItems": 10,
        "offset": 0
      }
    },
    "context": {
      "page": {
        "data": [],
        "type": "OTHER",
        "location": "https://www.carrefour.com.ar/",
        "locale": "es_AR"
      }
    },
    "selector": {
      "name": "Semantic Search"
    },
    "options": {
      "returnAnalyticsMetadata": false,
      "isImplicitClientData": false
    }
  },
  "_decisiones": [
    "`query.filters` se saca a propósito: el diagnóstico mide el motor CRUDO.",
    "Filtrar por categoría o precio daría un DY ajustado contra un VTEX sin",
    "ajustar, y la comparación no valdría nada.",
    "",
    "`locale` va es_AR y no el en_US de la plantilla: el idioma puede cambiar",
    "cómo se interpretan los términos, y acá se busca en español.",
    "",
    "`page.type` va OTHER y no HOMEPAGE: esto no es la home, es una consulta de",
    "diagnóstico. HOMEPAGE podría activar personalización de home y ensuciar los",
    "resultados.",
    "",
    "`numItems` 10 coincide con PAGE_SIZE de src/search-engines.js.",
    "",
    "El `dyid` es el que DEVOLVIÓ Dynamic Yield en la cookie _dyid_server de la",
    "primera llamada. Antes había uno inventado ('webdash-diagnostico-buscador') y",
    "DY respondía HTTP 200 con `choices: []` más el warning W084: \"The 'dyid' does",
    "not match the value created by Dynamic Yield\". O sea: aceptaba el request y no",
    "servía nada. Un id que DY no generó no le sirve.",
    "",
    "`session` se saca: la _dyjsession que devuelve DY vence a los 30 minutos",
    "(maxAge 1800), así que clavarla en un archivo no tiene sentido. DY crea una."
  ],
  "productsPath": "choices.0.variations.0.payload.data.slots",
  "totalPath": "choices.0.variations.0.payload.data.totalNumResults",
  "nameKey": "productData.name",
  "categoriesKey": "productData.categories",
  "_hallazgos_de_la_doc": [
    "1) El CORRECTOR DE TIPEOS de DY es SOLO EN INGLES: 'Available only in",
    "   English. The locale sent in the API request can't include other",
    "   languages.' Con locale es_AR queda desactivado. O sea que 'queso rayado'",
    "   (2.435 busquedas/mes, 0 resultados en VTEX) no lo va a arreglar el",
    "   spellchecker. Puede que el modelo semantico lo resuelva igual, porque",
    "   trabaja por embeddings y no por keyword exacta, pero eso hay que MEDIRLO,",
    "   no asumirlo. Es uno de los terminos del bake-off.",
    "",
    "   Se deja es_AR igual: el catalogo y las busquedas son en espanol, y sin",
    "   locale DY interpretaria los terminos como ingles, que es peor. Perder el",
    "   corrector de un idioma que no hablamos no cuesta nada.",
    "",
    "2) `pagination` es OBLIGATORIA en cada query de texto. Ya esta.",
    "",
    "3) `sortBy` NO se usa: la doc avisa que pisa por completo la logica de",
    "   relevancia. Medir relevancia con sortBy puesto no mediria nada.",
    "",
    "4) El locale agrega latencia: DY traduce las queries en tiempo real con un",
    "   LLM y despues las cachea. Las primeras corridas del bake-off van a ser",
    "   mas lentas que las siguientes.",
    "",
    "5) `facets` vuelve en cada respuesta si estan configuradas en Experience OS.",
    "   No se piden ni se usan para el diagnostico."
  ],
  "_confirmado": [
    "Endpoint, selector y rutas CONFIRMADOS contra la respuesta real (HTTP 200,",
    "choices: 1, 10 slots para 'leche').",
    "",
    "Dos cosas que la doc tenia distinto y la respuesta real corrigio:",
    "",
    "  1. La respuesta SI viene envuelta en `choices`. El ejemplo de la doc arranca",
    "     directo en la decision ({id, name, type, variations}); la real es",
    "     {choices: [{id, name, type, variations}]}.",
    "",
    "  2. Los slots NO traen solo el sku. El ejemplo de la doc muestra",
    "     {slotId, sku} y por eso se habia anotado que iba a haber que resolver",
    "     cada SKU contra VTEX para poder medir relevancia. La respuesta real trae",
    "     `productData` con name, categories, price y demas: se puede juzgar",
    "     relevancia directo, sin ninguna llamada extra.",
    "",
    "`facets` tambien vuelve en cada respuesta (los filtros disponibles con su",
    "conteo por categoria). No se usan para el diagnostico."
  ]
}
